\documentclass{article}

\usepackage{amsmath,amssymb}
\usepackage{xurl}
\usepackage[hidelinks]{hyperref}
\setlength{\emergencystretch}{2em}

\input{command}

\title{Scope of Ha's $A_\gamma$ Witness for Choi-Type Maps}
\author{}
\date{2026-08-24}

\begin{document}
\maketitle
\gapnote{scope-restriction}{wip}

\section{Source theorem and witness}

Wolf's Example~3.1 states that, for $d\ge3$ and $1\le n\le d-2$, the
Choi-type map
\begin{align}
    T_C(X)
     & =(d-n)D(X)-X+\sum_{k=1}^{n}D(U_{k0}XU_{k0}^{\dagger})
    \tag{3.20}
\end{align}
is positive and indecomposable\cite{Wolf2012Quantum}.
The local source is
\path{Notes/WolfNoteTexSource/ch03_positive_not_completely.tex}, lines
357--365. Wolf gives neither a proof nor a citation in that paragraph.

Ha proves the stronger atomicity assertion for this family
\cite[Theorem~2.1, pp.~593--595]{Ha1998AtomicPositiveMaps}. For
$0<\gamma<1$, the proof chooses all $3^d$-th roots of unity, forms vectors
$z_{r,i}$ that are 2-simple, and sets
\begin{align}
    A_r
     & =\frac{1}{3^d}\sum_{i=1}^{3^d}\ketbra{z_{r,i}}{z_{r,i}},
     &
    A_\gamma
     & =\frac1d\sum_{r=1}^{d}A_r.
    \label{eq:ha98_root_average}
\end{align}
Thus $A_\gamma$ belongs to the cone $V_2$ generated by projectors onto
2-simple vectors. Ha then derives the block entries of $A_\gamma$ from the
root power sum and the exponent-collision identity in equations (2.1)--(2.3).

Writing $A^T$ for Ha's block transpose, which is transposition in the second
tensor factor, the source next defines
\begin{align}
    u_i
     & =\frac{\gamma}{\sqrt d}e_{i+1}\otimes e_i
    +\frac{1}{\sqrt d\,\gamma}e_i\otimes e_{i+1}, \\
    v_i
     & =\sqrt{\frac{d-1}{d}}
    \bigl(e_{i+1}\otimes e_i+e_i\otimes e_{i+1}\bigr),
     &
    \alpha_i
     & =e_i\otimes e_i,
    \label{eq:ha98_uv_alpha}
\end{align}
with cyclic suffixes, together with the displayed vectors $\beta_{1j}$ and
$\beta_{ij}$ for non-neighbouring pairs. The calculation on p.~595 is
\begin{align}
    A_\gamma^T
     & =\sum_{i=1}^{d}
    \bigl(\ketbra{u_i}{u_i}+\ketbra{v_i}{v_i}
    +\ketbra{\alpha_i}{\alpha_i}\bigr)
    +\sum_{j=3}^{d-1}\ketbra{\beta_{1j}}{\beta_{1j}}
    +\sum_{i=2}^{d-2}\sum_{j=i+2}^{d}
    \ketbra{\beta_{ij}}{\beta_{ij}}.
    \label{eq:ha98_block_transpose}
\end{align}
It follows that the block transpose also belongs to $V_2$. Finally Ha
computes
\begin{align}
    \langle A_\gamma,T_C\rangle
     & =\gamma^2-1<0.
    \label{eq:ha98_negative_pairing}
\end{align}

\section{Formalized part}

The root family, the integers $m_k=\frac32(3^{k-1}-1)$, the vectors
$a_{i,k}$, $c_r$, $b_{r,i,j}$, and $z_{r,i}$, and the averages in
(\ref{eq:ha98_root_average}) are now defined with zero-based indices. The
cyclic coordinates use $\mathbb Z/d\mathbb Z$, so the shift convention agrees
with Wolf's equation (2.24) and Ha's rotation $S e_i=e_{i+1}$.

For fixed $r$ and $i$, every column of $z_{r,i}$ other than column $r$ is a
scalar multiple of $a_{i,1}$, while column $r$ is $c_r\circ a_{i,r}$.
Consequently the coefficient matrix has rank at most two. Averaging the
corresponding rank-one projectors proves
\begin{align}
    A_\gamma\in V_2,
    \qquad
    A_\gamma\ge0.
    \label{eq:ha98_formalized_slice}
\end{align}
This is Ha's first witness decomposition, not the PPT assertion and not the
indecomposability theorem.\footnote{Formal declarations:
    \leanid{Matrix.haTwoSimpleVector\_hasSchmidtRankLE\_two},
    \leanid{Matrix.haAGamma\_hasSchmidtNumberLE\_two}, and
    \leanid{Matrix.haAGamma\_posSemidef} in
    \doclink{QICLean/Channel/ChoiTypeMap/Indecomposable}.}

\section{Required orientation and remaining steps}

The bilinear duality used by Ha is fixed by Eom--Kye
\cite[Corollary~3.2 and Theorem~3.3(iii), pp.~136, 138--139]{EomKye2000Duality}.
If $\sigma$ swaps the two tensor factors and
$J_d=\sum_{i,j}e_{ij}\otimes e_{ij}$, their orientation is
\begin{align}
    \langle A,\Phi\rangle
     & =\bigl\langle(\Phi\otimes\operatorname{id})(A^\sigma),J_d\bigr\rangle.
    \label{eq:ha98_eom_kye_orientation}
\end{align}
Thus the state tested by the ampliation is $\rho=A_\gamma^\sigma$, not
$A_\gamma$. Since Ha's block transpose is the second-factor partial
transpose,
\begin{align}
    \rho^{T_1}
     & =\bigl(A_\gamma^{T_2}\bigr)^\sigma.
    \label{eq:ha98_partial_transpose_orientation}
\end{align}
Equations~(\ref{eq:ha98_formalized_slice}) and
(\ref{eq:ha98_block_transpose}) therefore give the two positive
semidefinite conditions for the factor-swapped PPT witness.

The maximally entangled vector in the formal development is normalized:
$\ket{\Omega}=d^{-1/2}\sum_i e_i\otimes e_i$. Hence Eom--Kye's matrix is
\begin{align}
    J_d
     & =d\ketbra{\Omega}{\Omega}.
    \label{eq:ha98_omega_normalization}
\end{align}
The positive factor $d$ must be retained when
(\ref{eq:ha98_negative_pairing}) is converted into a negative quadratic
form of $(T_C\otimes\operatorname{id})(\rho)$.

The remaining source-ordered work is therefore:
\begin{enumerate}
    \item prove Ha's root-average block formula (2.3), using the exact power-sum
          identity and exponent-collision relation;
    \item prove the rank-one identity (\ref{eq:ha98_block_transpose});
    \item transport it through the factor swap as in
          (\ref{eq:ha98_partial_transpose_orientation});
    \item prove the pairing identity (\ref{eq:ha98_eom_kye_orientation}) and
          normalization (\ref{eq:ha98_omega_normalization});
    \item calculate (\ref{eq:ha98_negative_pairing}) for the exact cyclic
          Choi-type map and apply the PPT witness criterion.
\end{enumerate}
The final assertion that the map is an indecomposable \emph{positive} map also
requires the full-range positivity theorem tracked by
\ghissue{19}. The witness calculation itself is independent of that final
conjunction and is tracked by \ghissue{18}.

\section{Classification}

\emph{Scope restriction.} The exact root-average construction and its
$V_2$ decomposition are formalized. The block-transpose decomposition,
factor-swapped PPT witness, Eom--Kye pairing, negative value, and
indecomposability conclusion remain open. No Choi-matrix or
partial-transpose ansatz is substituted for Ha's cited route.

\bibliographystyle{alpha}
\bibliography{references}

\end{document}
